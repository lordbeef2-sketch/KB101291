# NOMAGIC ATTACHMENT: example.zip

- attachment_id: `189149972`
- space_key: `TWCloud2024xR2`
- parent_page_id: `189149970`
- parent_page_title: Building Teamwork Cloud containers
- media_type: `application/zip`
- reported_bytes: 9245
- download_url: https://docs.nomagic.com/download/attachments/189149970/example.zip?version=1&modificationDate=1728044292755&api=v2
- payload_kind: `archive-expanded`
- downloaded_sha256: `a796c49baa0bf25c7888bf7563d37ddb5b031628344d80ca1f101446062bc00e`

## ARCHIVE CONTENTS

### ENTRY: README.md

- bytes: 2369
- crc32: `fcf59f9d`
- decoded_as: `utf-8`

````markdown
# Example with docker-compose

* Works with release 2021x Refresh2
* Using cassandra 3.11.10

## Purpose

The purpose - to provide example of TeamworkCloud Suite containerization with docker.

Using docker-compose as launch configuration for suite.

Two solutions provided:
1. Single cassandra and single teamworkcloud [here](#running-1-1-setup)
2. 3x cassandra cluster and 3x teamworkcloud cluster [here](#running-3-3-setup)

## Requirements

- docker and docker-compose
- java jdk 8 or newer (for key generating certificates)
- free ram for 1-1 solution `20G`:
  - zookeeper - `2G`
  - zoonagivator - `1G`
  - elasticsearch - `2G`
  - cassandra - `8G`
  - twcloud - `8G`
  - authserver - `1G`
  - webapp - `2G`
- free ram for 3-3 solution `20G`:
  - zookeeper - `2G`
  - zoonagivator - `1G`
  - elasticsearch - `2G`
  - cassandra - `1G`
  - cassandra2 - `1G`
  - cassandra3 - `1G` 
  - twcloud - `3G`
  - twcloud2 - `3G`
  - twcloud3 - `3G`
  - authserver - `1G`
  - webapp - `2G`
- have license key in license server available.

## Steps

### Running 1-1 setup

1. Download `twcloudsuite.zip` to this folder
   * name of file is important here.
2. Run command `expand_from_twcloudsuit.sh`
3. Run command `docker-compose build`
4. Run command `docker-compose up -d`

Apply license key to `https://host.docker.internal:8111`

To access webapp use `https://host.docker.internal:8443`

To connect from MagicDraw `host.docker.internal:3579`

* Command to stop `docker-compose stop`
* To stop and remove docker container: `docker-compose down`

### Running 3-3 setup

All steps as in 1-1 with one change to `docker-compose` commands - need to add `-f docker-compose.3-3.yaml`.

Eg. `docker-compose -f docker-compose.3-3.yaml build`

## Persistence

Some containers have data directories mapped to host for persistence between restarts. This directory is `data/{service_name}`.

Container that use `data` directory for persisting new data:
- cassandra
- cassandra2
- cassandra3
- elasticsearch
- webapp (only logs)
- zookeeper

For clean retry you may want to clear all of those directory contents except for `webapp/certs` - do not delete this.

## Advanced usage

### Existing cassandra database

It is possible to copy existing database contents to `data/cassandra` and running all systems.
Not yet tested. Might need some more properties changes for cassandra to start.
````

### ENTRY: authserver/Dockerfile

- bytes: 270
- crc32: `0b6d039c`
- decoded_as: `utf-8`

````text
FROM openjdk:11.0.11
COPY docker.scripts /scripts
RUN sh /scripts/init.sh
COPY AuthServer /authserver
RUN sh /scripts/setup.sh

EXPOSE 8555
ENV PUBLIC_HOSTNAME="localhost"
ENV TWC_SERVER_IP=""
ENV WEBAPP_SERVER_IP=""
WORKDIR /authserver
ENTRYPOINT /scripts/entrypoint.sh
````

### ENTRY: authserver/docker.scripts/entrypoint.sh

- bytes: 1040
- crc32: `57564389`
- decoded_as: `utf-8`

````text
#!/bin/bash
echo -----------------------------------------------------------
echo "PUBLIC_HOSTNAME=${PUBLIC_HOSTNAME}"
echo "TWC_SERVER_IP=${TWC_SERVER_IP}"
echo "WEBAPP_SERVER_IP=${WEBAPP_SERVER_IP}"

sed -i "s|\${server.ip}|${PUBLIC_HOSTNAME}|" config/authserver.properties
sed -i "s|\${twc.server.ip}|${TWC_SERVER_IP}|" config/authserver.properties
sed -i "s|\${webapp.server.ip}|${WEBAPP_SERVER_IP}|" config/authserver.properties
sed -i 's|^server.ssl.key-store=.*$|server.ssl.key-store=certs/authserver.p12|' config/authserver.properties
sed -i 's|^server.ssl.key-alias=.*$|server.ssl.key-alias=authserver|' config/authserver.properties
#sed -i 's|^server.ssl.trust-store=.*$|server.ssl.trust-store=certs/trust/truststore.jks|' config/authserver.properties
sed -i 's|^cassandra.contactPoints=.*$|cassandra.contactPoints=cassandra|' config/authserver.properties

pwd
ls -la
mkdir -p config/truststore

cp certs/trust/* config/truststore/

echo -----------------------------------------------------------
cd bin
chmod +x run.sh

./run.sh
````

### ENTRY: authserver/docker.scripts/init.sh

- bytes: 150
- crc32: `255bd7e4`
- decoded_as: `utf-8`

````text
#!/bin/bash
echo "=== INIT ==="

apt-get update && apt-get -y install apt-utils sudo wget netcat && rm -rf /var/lib/apt/lists/*

echo "/=== INIT ===/"
````

### ENTRY: authserver/docker.scripts/setup.sh

- bytes: 152
- crc32: `3710cdc5`
- decoded_as: `utf-8`

````text
#!/bin/bash
echo "=== SETUP ==="

mkdir -p /etc/twcloud
echo "TWCLOUD_HOME=$(pwd)
TWCLOUD_OWNR=`whoami`
JAVA_HOME=${JAVA_HOME}">/etc/twcloud/twcloud-env
````

### ENTRY: cassandra/Dockerfile

- bytes: 1003
- crc32: `ba41f77a`
- decoded_as: `utf-8`

````text
FROM cassandra:3.11.10

RUN sed -i 's/^thrift_framed_transport_size_in_mb: 15$/thrift_framed_transport_size_in_mb: 100/g' /etc/cassandra/cassandra.yaml
RUN sed -i 's/^batch_size_warn_threshold_in_kb: 5$/batch_size_warn_threshold_in_kb: 3000/g' /etc/cassandra/cassandra.yaml
RUN sed -i 's/^batch_size_fail_threshold_in_kb: 50$/batch_size_fail_threshold_in_kb: 5000/g' /etc/cassandra/cassandra.yaml
RUN sed -i 's/^read_request_timeout_in_ms: (10000|5000)$/read_request_timeout_in_ms: 600000/g' /etc/cassandra/cassandra.yaml
RUN sed -i 's/^range_request_timeout_in_ms: 10000$/range_request_timeout_in_ms: 600000/g' /etc/cassandra/cassandra.yaml
RUN sed -i 's/^write_request_timeout_in_ms: (10000|2000)$/write_request_timeout_in_ms: 600000/g' /etc/cassandra/cassandra.yaml
RUN sed -i 's/^truncate_request_timeout_in_ms: 60000$/truncate_request_timeout_in_ms: 600000/g' /etc/cassandra/cassandra.yaml
RUN sed -i 's/^request_timeout_in_ms: 10000$/request_timeout_in_ms: 600000/g' /etc/cassandra/cassandra.yaml

````

### ENTRY: docker-compose.3-3.yaml

- bytes: 5741
- crc32: `bb804470`
- decoded_as: `utf-8`

````yaml
version: '2.4'
services:
  zookeeper:
    image: zookeeper:3.7.0
    restart: unless-stopped
    mem_limit: 2g
    volumes:
      - ./data/zookeeper/data:/data
      - ./data/zookeeper/datalog:/datalog
      - ./data/zookeeper/logs:/logs
    healthcheck:
      test: [ "CMD", "echo", "ruok", "|", "nc -w2 zookeeper 4444" ]
      interval: 5s
      timeout: 10s
      retries: 3
  zoonavigator:
    image: elkozmon/zoonavigator:1.1.1
    restart: unless-stopped
    mem_limit: 1g
    ports:
      - "9000:9000"
    environment:
      - "CONNECTION_MYZK_CONN=zookeeper:2181"
      - "CONNECTION_LOCAL_CONN=localhost:2181"
      - "AUTO_CONNECT_CONNECTION_ID=MYZK"
    depends_on:
      zookeeper:
        condition: service_healthy
  elasticsearch:
    image: elasticsearch:7.16.2
    restart: unless-stopped
    mem_limit: 2g
    ports:
      - "9200:9200"
    environment:
      - discovery.type=single-node
      - cluster.routing.allocation.disk.threshold_enabled=false
      - "ES_JAVA_OPTS=-Xms2000M -Xmx2000M"
    volumes:
      - ./data/elasticsearch:/usr/share/elasticsearch/data
    healthcheck:
      test: [ "CMD", "nc", "-z", "-w2", "localhost", "9200"]
      interval: 5s
      timeout: 5s
      retries: 60
  cassandra:
    build: cassandra
    restart: unless-stopped
    mem_limit: 1g
    ports:
      - "9042:9042"
      - "9160:9160"
    environment:
      - CASSANDRA_SEEDS=cassandra
      - CASSANDRA_LISTEN_ADDRESS=cassandra
      - MAX_HEAP_SIZE=1000M
      - HEAP_NEWSIZE=100M
      - CASSANDRA_REPLICATION_FACTOR=3
    volumes:
      - ./data/cassandra:/var/lib/cassandra
    healthcheck:
      test: [ "CMD", "cqlsh", "-e", "describe keyspaces" ]
      interval: 5s
      timeout: 5s
      retries: 60
  cassandra2:
    build: cassandra
    restart: unless-stopped
    mem_limit: 1g
    ports:
      - "9043:9042"
      - "9161:9160"
    environment:
      - CASSANDRA_SEEDS=cassandra
      - CASSANDRA_LISTEN_ADDRESS=cassandra2
      - MAX_HEAP_SIZE=1000M
      - HEAP_NEWSIZE=100M
    volumes:
      - ./data/cassandra2:/var/lib/cassandra
    healthcheck:
      test: [ "CMD", "cqlsh", "-e", "describe keyspaces" ]
      interval: 5s
      timeout: 5s
      retries: 60
    depends_on:
      cassandra:
        condition: service_healthy
  cassandra3:
    build: cassandra
    restart: unless-stopped
    mem_limit: 1g
    ports:
      - "9044:9042"
      - "9162:9160"
    environment:
      - CASSANDRA_SEEDS=cassandra
      - CASSANDRA_LISTEN_ADDRESS=cassandra3
      - MAX_HEAP_SIZE=1000M
      - HEAP_NEWSIZE=100M
    volumes:
      - ./data/cassandra3:/var/lib/cassandra
    healthcheck:
      test: [ "CMD", "cqlsh", "-e", "describe keyspaces" ]
      interval: 5s
      timeout: 5s
      retries: 60
    depends_on:
      cassandra:
        condition: service_healthy
  twcloud:
    build: twcloud
    restart: unless-stopped
    mem_limit: 3g
    ports:
      - "8111:8111"
      - "3579:3579"
    environment:
      - CASSANDRA_HOST=cassandra
      - ELASTIC_HOST=elasticsearch
      - TWC_SEEDS=twcloud
      - AUTHSERVER_URL=https://host.docker.internal:8555
      - TWC_XMX=3g
    volumes:
      - ./data/twcloud/certs:/twcloud/certs
    healthcheck:
      test: [ "CMD", "nc", "-z", "-w2", "localhost", "3579" ]
      interval: 5s
      timeout: 5s
      retries: 60
    depends_on:
      cassandra:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
  twcloud2:
    build: twcloud
    restart: unless-stopped
    mem_limit: 3g
    ports:
      - "8112:8111"
      - "3580:3579"
    environment:
      - CASSANDRA_HOST=cassandra
      - ELASTIC_HOST=elasticsearch
      - TWC_SEEDS=twcloud
      - AUTHSERVER_URL=https://host.docker.internal:8555
      - TWC_XMX=3g
    volumes:
      - ./data/twcloud2/certs:/twcloud/certs
    healthcheck:
      test: [ "CMD", "nc", "-z", "-w2", "localhost", "3579" ]
      interval: 5s
      timeout: 5s
      retries: 60
    depends_on:
      cassandra:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
      twcloud:
        condition: service_healthy
  twcloud3:
    build: twcloud
    restart: unless-stopped
    mem_limit: 3g
    ports:
      - "8113:8111"
      - "3581:3579"
    environment:
      - CASSANDRA_HOST=cassandra
      - ELASTIC_HOST=elasticsearch
      - TWC_SEEDS=twcloud
      - AUTHSERVER_URL=https://host.docker.internal:8555
      - TWC_XMX=3g
    volumes:
      - ./data/twcloud3/certs:/twcloud/certs
    healthcheck:
      test: [ "CMD", "nc", "-z", "-w2", "localhost", "3579" ]
      interval: 5s
      timeout: 5s
      retries: 60
    depends_on:
      cassandra:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
      twcloud:
        condition: service_healthy
  authserver:
    build: authserver
    restart: unless-stopped
    mem_limit: 1g
    ports:
      - "8555:8555"
    environment:
      - PUBLIC_HOSTNAME=host.docker.internal
      - TWC_SERVER_IP=host.docker.internal
      - WEBAPP_SERVER_IP=host.docker.internal
    volumes:
      - ./data/authserver/certs:/authserver/certs
  webapp:
    build: webapp
    restart: unless-stopped
    mem_limit: 2g
    ports:
      - "8443:8443"
    environment:
      - TWC_HOST=twcloud
      - ZOOKEEPER_HOST=zookeeper
      - AUTHSERVER_HOST=host.docker.internal
      - AUTHSERVER_PORT=8555
      - AUTHSERVER_PROTOCOL=https
      - AUTHSERVER_PATH=authentication
    volumes:
      - ./data/webapp/certs:/webapp/certs
      - ./data/webapp/logs:/webapp/logs
    healthcheck:
      test: [ "CMD", "nc", "-z", "-w2", "localhost", "8443" ]
      interval: 5s
      timeout: 5s
      retries: 60
    depends_on:
      twcloud:
        condition: service_healthy
````

### ENTRY: docker-compose.yml

- bytes: 3154
- crc32: `73950903`
- decoded_as: `utf-8`

````yaml
version: '2.4'
services:
  zookeeper:
    image: zookeeper:3.7.0
    restart: unless-stopped
    mem_limit: 2g
    volumes:
      - ./data/zookeeper/data:/data
      - ./data/zookeeper/datalog:/datalog
      - ./data/zookeeper/logs:/logs
    healthcheck:
      test: [ "CMD", "echo", "ruok", "|", "nc -w 2 zookeeper 4444" ]
      interval: 5s
      timeout: 10s
      retries: 3
  zoonavigator:
    image: elkozmon/zoonavigator:1.1.1
    restart: unless-stopped
    ports:
      - "9000:9000"
    environment:
      - "CONNECTION_MYZK_CONN=zookeeper:2181"
      - "CONNECTION_LOCAL_CONN=localhost:2181"
      - "AUTO_CONNECT_CONNECTION_ID=MYZK"
    depends_on:
      zookeeper:
        condition: service_healthy
  elasticsearch:
    image: elasticsearch:7.16.2
    restart: unless-stopped
    mem_limit: 2g
    ports:
      - "9200:9200"
    environment:
      - discovery.type=single-node
      - cluster.routing.allocation.disk.threshold_enabled=false
      - "ES_JAVA_OPTS=-Xms2000M -Xmx2000M"
    volumes:
      - ./data/elasticsearch:/usr/share/elasticsearch/data
    healthcheck:
      test: [ "CMD", "nc", "-z", "-w2", "localhost", "9200" ]
      interval: 5s
      timeout: 5s
      retries: 60
  cassandra:
    build: cassandra
    restart: unless-stopped
    mem_limit: 8g
    ports:
      - "9042:9042"
      - "9160:9160"
    environment:
      - CASSANDRA_SEEDS=cassandra
      - CASSANDRA_LISTEN_ADDRESS=cassandra
      - MAX_HEAP_SIZE=8000M
      - HEAP_NEWSIZE=100M
    volumes:
      - ./data/cassandra:/var/lib/cassandra
    healthcheck:
      test: [ "CMD", "cqlsh", "-e", "describe keyspaces" ]
      interval: 5s
      timeout: 5s
      retries: 60
  authserver:
    build: authserver
    mem_limit: 1g
    ports:
      - "8555:8555"
    environment:
      - PUBLIC_HOSTNAME=host.docker.internal
      - TWC_SERVER_IP=host.docker.internal
      - WEBAPP_SERVER_IP=host.docker.internal
    volumes:
      - ./data/authserver/certs:/authserver/certs
  twcloud:
    build: twcloud
    mem_limit: 8400m
    ports:
      - "8111:8111"
      - "3579:3579"
    environment:
      - CASSANDRA_HOST=cassandra
      - ELASTIC_HOST=elasticsearch
      - TWC_SEEDS=localhost
      - AUTHSERVER_URL=https://host.docker.internal:8555
    volumes:
      - ./data/twcloud/certs:/twcloud/certs
    healthcheck:
      test: [ "CMD", "nc", "-z", "-w2", "localhost", "3579" ]
      interval: 5s
      timeout: 5s
      retries: 60
    depends_on:
      cassandra:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
  webapp:
    build: webapp
    mem_limit: 2100m
    ports:
      - "8443:8443"
    environment:
      - TWC_HOST=twcloud
      - ZOOKEEPER_HOST=zookeeper
      - AUTHSERVER_HOST=host.docker.internal
      - AUTHSERVER_PORT=8555
      - AUTHSERVER_PROTOCOL=https
      - AUTHSERVER_PATH=authentication
    volumes:
      - ./data/webapp/certs:/webapp/certs
      - ./data/webapp/logs:/webapp/logs
    healthcheck:
      test: [ "CMD", "nc", "-z", "-w2", "localhost", "8443" ]
      interval: 5s
      timeout: 5s
      retries: 60
    depends_on:
      twcloud:
        condition: service_healthy

````

### ENTRY: expand_from_twcloudsuit.sh

- bytes: 743
- crc32: `c879ed9c`
- decoded_as: `utf-8`

````text
#!/usr/bin/env bash

unzip -q twcloudsuite.zip
# extracted locally to 'TeamworkCloudSuite'

cp -r TeamworkCloudSuite twcloud

cp -r TeamworkCloudSuite/AuthServer authserver

cp -r TeamworkCloudSuite/WebAppPlatform webapp

mkdir -p data/twcloud

mkdir -p data/authserver

bash generateCerts.sh

mkdir -p data/twcloud/certs/trust
cp data/certs/twcloud.p12 data/twcloud/certs
cp data/certs/*.crt data/twcloud/certs/trust
cp -r data/twcloud data/twcloud2
cp -r data/twcloud data/twcloud3

mkdir -p data/authserver/certs/trust
cp data/certs/authserver.p12 data/authserver/certs
cp data/certs/*.crt data/authserver/certs/trust/

mkdir -p data/webapp/certs/trust
cp data/certs/webapp.p12 data/webapp/certs
cp data/certs/*.crt data/webapp/certs/trust

````

### ENTRY: generateCerts.sh

- bytes: 633
- crc32: `c02fce1d`
- decoded_as: `utf-8`

````text
#!/usr/bin/env bash

mkdir -p data/certs

generate() {
  local fqdn=$1

  echo Generate keypair with -dname cn=$fqdn -ext SAN=dns:$fqdn

keytool -genkeypair -keyalg RSA -sigalg SHA256withRSA -keysize 2048 -validity 3650 -keystore "${fqdn}.p12" \
 -storetype  pkcs12 -storepass nomagic -keypass nomagic -alias $fqdn -dname cn=$fqdn \
 -ext BasicConstraints:critical=ca:false -ext SAN=dns:$fqdn,dns:localhost,dns:host.docker.internal
keytool -export -keystore "${fqdn}.p12" -storepass nomagic -alias ${fqdn} -file "${fqdn}.crt"

}

pushd data/certs && \
generate twcloud && \
generate authserver && \
generate webapp && \
popd || exit

````

### ENTRY: twcloud/Dockerfile

- bytes: 200
- crc32: `399df0c4`
- decoded_as: `utf-8`

````text
FROM openjdk:11.0.11
COPY docker.scripts /scripts
RUN sh /scripts/init.sh
COPY TeamworkCloudSuite /twcloud
WORKDIR /twcloud
RUN sh /scripts/setup.sh
EXPOSE 3579 8111
ENTRYPOINT /scripts/entrypoint.sh

````

### ENTRY: twcloud/docker.scripts/entrypoint.sh

- bytes: 3157
- crc32: `5338710f`
- decoded_as: `utf-8`

````text
#!/bin/bash
echo "CASSANDRA_HOST=${CASSANDRA_HOST}"
echo "TWCLOUD_HOME=${TWCLOUD_HOME}"
echo "TWCLOUD_OWNR=${TWCLOUD_OWNR}"
echo "JAVA_HOME=${JAVA_HOME}"
echo "ELASTIC_HOST=${ELASTIC_HOST}"
echo "TWC_XMX=${TWC_XMX}"
echo "TWC_SEEDS=${TWC_SEEDS}"
echo "TWC_ZOOKEEPER_URL=${TWC_ZOOKEEPER_URL}"
echo "TWC_ZOOKEEPER_PATH=${TWC_ZOOKEEPER_PATH}"
echo "CASSANDRA_REPLICATION_FACTOR=${CASSANDRA_REPLICATION_FACTOR}"
echo "AUTHSERVER_URL=${AUTHSERVER_URL}"

free -h

java -version

CASSANDRA_HOSTS=""
IFS=',' read -ra CASS <<< "$CASSANDRA_HOST"
C1=true
for i in "${CASS[@]}"; do
  if [ $C1 == true ]
    then
      CASSANDRA_HOSTS=${CASSANDRA_HOSTS}\"${i}
      C1=false
  else
      CASSANDRA_HOSTS=${CASSANDRA_HOSTS}\",\"${i}
  fi
done
CASSANDRA_HOSTS=$CASSANDRA_HOSTS\"

echo "CASSANDRA_HOSTS=${CASSANDRA_HOSTS}"

TWC_AKKA_SEEDS=""
IFS=',' read -ra SEED <<< "$TWC_SEEDS"
T1=true

for i in "${SEED[@]}"; do
  if [ $T1 == true ]
    then
      TWC_AKKA_SEEDS=$TWC_AKKA_SEEDS\"akka://twcloud@$i:2552
      T1=false
  else
    TWC_AKKA_SEEDS=$TWC_AKKA_SEEDS\",\"akka://twcloud@$i:2552
  fi
done
TWC_AKKA_SEEDS=$TWC_AKKA_SEEDS\"

echo "TWC_AKKA_SEEDS=$TWC_AKKA_SEEDS"

while ! nc -z -w2 ${ELASTIC_HOST} 9200; do echo "." && sleep 0.1; done

pwd -P
ls -la

#sed -i 's/contact-points = \[\"127.0.0.1:9042\"\]/contact-points = \['${CASSANDRA_HOSTS}'\]/' configuration/application.conf
sed -i 's/seeds = \[\"localhost\"\]/seeds = \['${CASSANDRA_HOSTS}'\]/' configuration/application.conf
if [ ! -z ${CASSANDRA_REPLICATION_FACTOR} ]
  then
    sed -i 's/replication-factor = 1/replication-factor = '${CASSANDRA_REPLICATION_FACTOR}'/' configuration/application.conf
    echo "Apply replication factor."
fi
if [ ! -z ${TWC_XMX} ]
  then
    sed -ri "s/-Xmx=[.*]/-Xmx=${TWC_XMX}/" jvm.options
fi
if [ ! -z ${TWC_ZOOKEEPER_URL} ]
  then
    echo "-Desi.zookeeper.enabled=true" >> jvm.options
    echo "-Desi.zookeeper.url=\"${TWC_ZOOKEEPER_URL}\"" >> jvm.options
    echo "-Dakka.cluster.seed.zookeeper.path=\"${TWC_ZOOKEEPER_PATH}\"" >> jvm.options
  else
    sed -ri 's%# seed-nodes = \["[^"]+"\]%seed-nodes = ['${TWC_AKKA_SEEDS}']%' configuration/application.conf
fi
echo "-Desi.query.es.node.host=\"${ELASTIC_HOST}\"" >> jvm.options
echo "-Desi.indexer.enabled=true" >> jvm.options
echo "-Desi.server.actor.query.component-enabled=true" >>	jvm.options
if [ -f "./license/CEDW_SEAT_TEST_CUSTOM.txt" ]
  then
    echo "License file provided."
    sed -i 's/\r//g' license/CEDW_SEAT_TEST_CUSTOM.txt
    echo "-Dkeyfile.location=./license/CEDW_SEAT_TEST_CUSTOM.txt" >> jvm.options
fi
echo "-Desi.httpd.ssl.keystorePath=certs/twcloud.p12" >> jvm.options
echo "-Desi.auth.https.file=certs/trust/authserver.crt" >> jvm.options
echo "-Desi.auth.url=${AUTHSERVER_URL}" >> jvm.options
echo -----------------------------------------------------------
sed -i 's/\r//g' twcloud
sed -i 's/\r//g' jvm.options
sed -i 's/\r//g' configuration/application.conf
cat configuration/application.conf
echo -----------------------------------------------------------
cat jvm.options
echo -----------------------------------------------------------
echo "Starting..."
./twcloud
echo "Exiting... $?"

````

### ENTRY: twcloud/docker.scripts/init.sh

- bytes: 150
- crc32: `255bd7e4`
- decoded_as: `utf-8`

````text
#!/bin/bash
echo "=== INIT ==="

apt-get update && apt-get -y install apt-utils sudo wget netcat && rm -rf /var/lib/apt/lists/*

echo "/=== INIT ===/"
````

### ENTRY: twcloud/docker.scripts/setup.sh

- bytes: 185
- crc32: `475c24a2`
- decoded_as: `utf-8`

````text
#!/bin/bash
echo "=== SETUP ==="

sed -i 's|<!-- <appender-ref ref=\"SERVER-CONSOLE\"\/> -->|<appender-ref ref=\"SERVER-CONSOLE\"\/>|' configuration/logback.xml

echo "/=== SETUP ===/"

````

### ENTRY: webapp/Dockerfile

- bytes: 189
- crc32: `96a7b11a`
- decoded_as: `utf-8`

````text
FROM openjdk:11.0.11
COPY docker.scripts /scripts
RUN sh /scripts/init.sh
COPY WebAppPlatform /webapp
RUN sh /scripts/setup.sh
WORKDIR /webapp
EXPOSE 8443
ENTRYPOINT /scripts/entrypoint.sh

````

### ENTRY: webapp/docker.scripts/entrypoint.sh

- bytes: 980
- crc32: `2830be8a`
- decoded_as: `utf-8`

````text
#!/bin/bash
echo "TWC_HOST=${TWC_HOST}"
echo "ZOOKEEPER_HOST=${ZOOKEEPER_HOST}"
echo "AUTHSERVER_HOST=${AUTHSERVER_HOST}"
echo "AUTHSERVER_PORT=${AUTHSERVER_PORT}"
echo "AUTHSERVER_PROTOCOL=${AUTHSERVER_PROTOCOL}"
echo "AUTHSERVER_PATH=${AUTHSERVER_PATH}"

AUTHSERVER_URI=$AUTHSERVER_PROTOCOL://$AUTHSERVER_HOST:$AUTHSERVER_PORT/$AUTHSERVER_PATH

sed -i "s|^twc.ip=.*$|twc.ip=${TWC_HOST}|" shared/conf/webappplatform.properties
sed -i "s|^zookeeper.connect.string=.*$|zookeeper.connect.string=${ZOOKEEPER_HOST}|" shared/conf/webappplatform.properties
sed -i "s|^authentication.server.uri=.*$|authentication.server.uri=${AUTHSERVER_URI}|" shared/conf/webappplatform.properties

echo "ignore.wap.twc.compatibility=true" >> shared/conf/webappplatform.properties

sed -i 's|certificateKeystoreFile="../configuration/keystore.p12"|certificateKeystoreFile="certs/webapp.p12"|' conf/server.xml

echo -----------------------------------------------------------

bash ./bin/catalina.sh run
````

### ENTRY: webapp/docker.scripts/init.sh

- bytes: 150
- crc32: `255bd7e4`
- decoded_as: `utf-8`

````text
#!/bin/bash
echo "=== INIT ==="

apt-get update && apt-get -y install apt-utils sudo wget netcat && rm -rf /var/lib/apt/lists/*

echo "/=== INIT ===/"
````

### ENTRY: webapp/docker.scripts/setup.sh

- bytes: 57
- crc32: `6846ceea`
- decoded_as: `utf-8`

````text
#!/bin/bash
echo "=== SETUP ==="

echo "/=== SETUP ===/"

````


## EXACT ATTACHMENT METADATA

````json
{
  "id": "189149972",
  "type": "attachment",
  "status": "current",
  "title": "example.zip",
  "version": {
    "by": {
      "type": "known",
      "username": "ingabe",
      "userKey": "ff80808151f3008c0152c05ba1a40003",
      "profilePicture": {
        "path": "/download/attachments/7415957/user-avatar",
        "width": 48,
        "height": 48,
        "isDefault": false
      },
      "displayName": "Inga A.",
      "_links": {
        "self": "https://docs.nomagic.com/rest/api/user?key=ff80808151f3008c0152c05ba1a40003"
      },
      "_expandable": {
        "status": ""
      }
    },
    "when": "2024-10-04T14:18:12.755+02:00",
    "message": "",
    "number": 1,
    "minorEdit": true,
    "hidden": false,
    "_links": {
      "self": "https://docs.nomagic.com/rest/experimental/content/189149972/version/1"
    },
    "_expandable": {
      "content": "/rest/api/content/189149972"
    }
  },
  "position": -1,
  "container": {
    "id": "189149970",
    "type": "page",
    "status": "current",
    "title": "Building Teamwork Cloud containers",
    "position": 0,
    "extensions": {
      "position": 0
    },
    "_links": {
      "webui": "/spaces/TWCloud2024xR2/pages/189149970/Building+Teamwork+Cloud+containers",
      "edit": "/pages/resumedraft.action?draftId=189149970",
      "tinyui": "/x/EjNGCw",
      "self": "https://docs.nomagic.com/rest/api/content/189149970"
    },
    "_expandable": {
      "container": "/rest/api/space/TWCloud2024xR2",
      "metadata": "",
      "operations": "",
      "children": "/rest/api/content/189149970/child",
      "restrictions": "/rest/api/content/189149970/restriction/byOperation",
      "history": "/rest/api/content/189149970/history",
      "ancestors": "",
      "body": "",
      "version": "",
      "descendants": "/rest/api/content/189149970/descendant",
      "space": "/rest/api/space/TWCloud2024xR2",
      "relevantViewRestrictions": "/rest/api/content/189149970/restriction/relevantViewRestrictions"
    }
  },
  "metadata": {
    "mediaType": "application/zip"
  },
  "extensions": {
    "mediaType": "application/zip",
    "fileSize": 9245,
    "comment": ""
  },
  "_links": {
    "download": "/download/attachments/189149970/example.zip?version=1&modificationDate=1728044292755&api=v2",
    "webui": "/spaces/TWCloud2024xR2/pages/189149970/Building+Teamwork+Cloud+containers?preview=%2F189149970%2F189149972%2Fexample.zip",
    "self": "https://docs.nomagic.com/rest/api/content/189149972"
  },
  "_expandable": {
    "operations": "",
    "children": "/rest/api/content/189149972/child",
    "restrictions": "/rest/api/content/189149972/restriction/byOperation",
    "history": "/rest/api/content/189149972/history",
    "ancestors": "",
    "body": "",
    "descendants": "/rest/api/content/189149972/descendant",
    "space": "/rest/api/space/TWCloud2024xR2",
    "relevantViewRestrictions": "/rest/api/content/189149972/restriction/relevantViewRestrictions"
  }
}
````
