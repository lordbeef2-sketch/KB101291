# NI DOCUMENT BUNDLE: lvdfdt-api-ref

<!--NI_BUNDLE_CHUNK bundle=lvdfdt-api-ref start=1 end=18 -->
<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/codegen-llb/dfd-fxp-nstage-mrate-code-generator-vi.html language=enus -->
## TOPIC 00001: DFD FXP NStage MRate Code Generator VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/codegen-llb/dfd-fxp-nstage-mrate-code-generator-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/codegen-llb/dfd-fxp-nstage-mrate-code-generator-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Generates LabVIEW field-programmable gate array (FPGA) code from a multistage multirate filter. icon Inputs/Outputs cbool.png open project? (F) open project? specifies if this VI opens the project file after generating the code. The default is FALSE, which means that you must open the project file m

### DFD FXP NStage MRate Code Generator VI

Generates LabVIEW field-programmable gate array (FPGA) code from a multistage multirate filter.

[IMAGE alt='icon' src='dfd-fxp-nstage-mrate-code-generator-vi.png']

#### Inputs/Outputs

| open project? (F) — open project? specifies if this VI opens the project file after generating the code. The default is FALSE, which means that you must open the project file manually after this VI generates the code. # channels — # channels specifies the number of channels that you want the generated code to process. The valid range is [1, 255]. The default is 1. Note The valid range is [1, 32] if the multistage multirate filter has one or more cascaded integrator comb (CIC) filter stages. multirate filters — multirate filters specifies the input multistage multirate filter. mMRate_Structure — specifies the implementation structure of the multirate filter. It could be one of the following values 0 -- CIC 1 -- FIR 2 -- IIR (Reserved for future implementation) 3 -- Lattice (Reserved for future implementation mMRate_Mode — specifies the operation mode for the multirate filter. mode has five options. -1 -- Default 0 -- No Rate Change 1 -- Interpolation 2 -- Decimation 3 -- Interpolated mMRate_Factor — is the sampling rate conversion factor of the multirate filter. mMRate_FIRParams — multirate filters specifies the input multistage multirate filter. mMRate_FIR_H — specifies the coefficients of an FIR filter. mMRate_FIR_IsNyquist? — (reserved for future usage) indicates whether the filter is a Nyquist (halfband) filter. It could be used to increase the performance since the Nyquist (halfband) filter is zero interleaved. mMRate_FIR_IsSymmetric? — (reserved for future usage) indicates whether the filter has even symmetry, odd symmetry or neither of them. It could be used to increase the performance. mMRate_CICParams — Is the parameters which describe the CIC filter. mMRate_CIC_#Stage — specifies the stages of the CIC filter. mMRate_CIC_Lag — is the differential delay of the CIC filter. mMRate_CIC_Type — specifies the passband of the CIC filter. filter type has two possible options. 0 -- Lowpass (default) 1 -- Highpass mMRate_Order — specifies the order of the multirate filter. mMRate_Version — multirate filters specifies the input multistage multirate filter. mMRate_Comments — multirate filters specifies the input multistage multirate filter. mMRate_RationalFactor — multirate filters specifies the input multistage multirate filter. mMRate_L — multirate filters specifies the input multistage multirate filter. mMRate_M — multirate filters specifies the input multistage multirate filter. mMRate_Fs — multirate filters specifies the input multistage multirate filter. mMRate_Quantized? — multirate filters specifies the input multistage multirate filter. mMRate_QFilterGain — multirate filters specifies the input multistage multirate filter. mMRate_CoefWl — multirate filters specifies the input multistage multirate filter. mMRate_Scaling — multirate filters specifies the input multistage multirate filter. mMRate_ScaleByPowerOf2? — multirate filters specifies the input multistage multirate filter. mMRate_ScaleType — multirate filters specifies the input multistage multirate filter. mMRate_InputWl — multirate filters specifies the input multistage multirate filter. mMRate_OutputWl — multirate filters specifies the input multistage multirate filter. output round-off mode — specifies the round-off mode in the quantizer. It has the following five options: 0 - Nearest 1 - Truncation 2 - +Inf 3 - -Inf 4 - Zero The default is Truncation. mMRate_MultMode — multirate filters specifies the input multistage multirate filter. mMRate_BitPruning — multirate filters specifies the input multistage multirate filter. mMRate_GainWl — multirate filters specifies the input multistage multirate filter. mMRate_OutputFwl — multirate filters specifies the input multistage multirate filter. mMRate_SumIwl — multirate filters specifies the input multistage multirate filter. mMRate_InputIwl — multirate filters specifies the input multistage multirate filter. mMRate_SumWl — multirate filters specifies the input multistage multirate filter. mMRate_CoefIwl — multirate filters specifies the input multistage multirate filter. destination folder — destination folder specifies the path to the folder in which you want to save the generated code. This VI returns an error if you do not specify a valid path to the folder. filter name — filter name specifies a name for the multistage multirate filter code that this VI generates. This VI also uses this value as the filename of the project file that contains the generated filter code. You can use only letters and digits in the filter name input. This VI ignores other characters. If you specify an invalid name, this VI creates a string that starts with Unknown. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. confirm? (T) — confirm? specifies if you want this VI to ask you for confirmation before replacing an existing file. If the value is TRUE, this VI displays a dialog box asking for confirmation to replace the existing file. If the value is FALSE, this VI replaces the existing file automatically. The default is TRUE. lvproj path — lvproj path returns the path to the generated project file. sampling frequency/FPGA clock — sampling frequency/FPGA clock returns a ratio. You can multiply this ratio with a specific FPGA clock rate to calculate the maximum input sampling frequency per channel that the generated FPGA code can process at the FPGA clock rate. For example, if the ratio is 0. 05 and the FPGA clock rate is 40 MHz, then the maximum input sampling frequency per channel that the generated FPGA code can process is 2 MHz. error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| mMRate_Structure — specifies the implementation structure of the multirate filter. It could be one of the following values 0 -- CIC 1 -- FIR 2 -- IIR (Reserved for future implementation) 3 -- Lattice (Reserved for future implementation mMRate_Mode — specifies the operation mode for the multirate filter. mode has five options. -1 -- Default 0 -- No Rate Change 1 -- Interpolation 2 -- Decimation 3 -- Interpolated mMRate_Factor — is the sampling rate conversion factor of the multirate filter. mMRate_FIRParams — multirate filters specifies the input multistage multirate filter. mMRate_FIR_H — specifies the coefficients of an FIR filter. mMRate_FIR_IsNyquist? — (reserved for future usage) indicates whether the filter is a Nyquist (halfband) filter. It could be used to increase the performance since the Nyquist (halfband) filter is zero interleaved. mMRate_FIR_IsSymmetric? — (reserved for future usage) indicates whether the filter has even symmetry, odd symmetry or neither of them. It could be used to increase the performance. mMRate_CICParams — Is the parameters which describe the CIC filter. mMRate_CIC_#Stage — specifies the stages of the CIC filter. mMRate_CIC_Lag — is the differential delay of the CIC filter. mMRate_CIC_Type — specifies the passband of the CIC filter. filter type has two possible options. 0 -- Lowpass (default) 1 -- Highpass mMRate_Order — specifies the order of the multirate filter. mMRate_Version — multirate filters specifies the input multistage multirate filter. mMRate_Comments — multirate filters specifies the input multistage multirate filter. mMRate_RationalFactor — multirate filters specifies the input multistage multirate filter. mMRate_L — multirate filters specifies the input multistage multirate filter. mMRate_M — multirate filters specifies the input multistage multirate filter. mMRate_Fs — multirate filters specifies the input multistage multirate filter. mMRate_Quantized? — multirate filters specifies the input multistage multirate filter. mMRate_QFilterGain — multirate filters specifies the input multistage multirate filter. mMRate_CoefWl — multirate filters specifies the input multistage multirate filter. mMRate_Scaling — multirate filters specifies the input multistage multirate filter. mMRate_ScaleByPowerOf2? — multirate filters specifies the input multistage multirate filter. mMRate_ScaleType — multirate filters specifies the input multistage multirate filter. mMRate_InputWl — multirate filters specifies the input multistage multirate filter. mMRate_OutputWl — multirate filters specifies the input multistage multirate filter. output round-off mode — specifies the round-off mode in the quantizer. It has the following five options: 0 - Nearest 1 - Truncation 2 - +Inf 3 - -Inf 4 - Zero The default is Truncation. mMRate_MultMode — multirate filters specifies the input multistage multirate filter. mMRate_BitPruning — multirate filters specifies the input multistage multirate filter. mMRate_GainWl — multirate filters specifies the input multistage multirate filter. mMRate_OutputFwl — multirate filters specifies the input multistage multirate filter. mMRate_SumIwl — multirate filters specifies the input multistage multirate filter. mMRate_InputIwl — multirate filters specifies the input multistage multirate filter. mMRate_SumWl — multirate filters specifies the input multistage multirate filter. mMRate_CoefIwl — multirate filters specifies the input multistage multirate filter. |
| mMRate_FIR_H — specifies the coefficients of an FIR filter. mMRate_FIR_IsNyquist? — (reserved for future usage) indicates whether the filter is a Nyquist (halfband) filter. It could be used to increase the performance since the Nyquist (halfband) filter is zero interleaved. mMRate_FIR_IsSymmetric? — (reserved for future usage) indicates whether the filter has even symmetry, odd symmetry or neither of them. It could be used to increase the performance. |
| mMRate_CIC_#Stage — specifies the stages of the CIC filter. mMRate_CIC_Lag — is the differential delay of the CIC filter. mMRate_CIC_Type — specifies the passband of the CIC filter. filter type has two possible options. 0 -- Lowpass (default) 1 -- Highpass |
| mMRate_L — multirate filters specifies the input multistage multirate filter. mMRate_M — multirate filters specifies the input multistage multirate filter. |
| mMRate_ScaleByPowerOf2? — multirate filters specifies the input multistage multirate filter. mMRate_ScaleType — multirate filters specifies the input multistage multirate filter. |

#### DFD FXP NStage MRate Code Generator Details

The generated multirate filter block does not have front panel indicators or controls. The generated multirate filter block has two FIFOs, **filtername_DataIn** and **filtername_DataOut**, to communicate with the data source and the destination of the filtered signal. For a multi-channel multirate filter, data from multiple channels is interleaved into a single input. You are responsible for controlling the data sequence into/out of the filter block.

#### Examples

Refer to the Generate LabVIEW FPGA Code for Multistage Multirate Filter VI in the labview\examples\Digital Filter Design\Fixed-Point Filters\Multirate directory for an example of using the DFD FXP NStage MRate Code Generator VI.

Parent topic:

Multirate Fixed-Point Tools

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-arbitrary-group-delay-design-vi.html language=enus -->
## TOPIC 00002: DFD Arbitrary Group Delay Design VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-arbitrary-group-delay-design-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-arbitrary-group-delay-design-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Creates an allpass filter with a group delay that you specify. The group delay in the new filter is optimal in terms of the least p^th norm. icon Inputs/Outputs cdbl.png pole radius constraint pole radius constraint specifies the maximum pole radius for the new filter. The default is 0.99. A small p

### DFD Arbitrary Group Delay Design VI

Creates an allpass filter with a group delay that you specify.

The group delay in the new filter is optimal in terms of the least p<sup>th</sup> norm.

[IMAGE alt='icon' src='dfd-arbitrary-group-delay-design-vi.png']

#### Inputs/Outputs

| pole radius constraint — pole radius constraint specifies the maximum pole radius for the new filter. The default is 0.99. A small pole radius constraint decreases the possibility of filter instability caused by finite precision effects. However, a small value of the pole radius constraint can adversely affect the potential sharpness of the group delay response. The valid values for pole radius constraint are within the range (0, 1]. This VI ignores the input and applies no constraint to the radius if a value is beyond the range. integer offset? (T) — integer offset? specifies whether the group delay offset between the group delay of the filter and predefined specifications is an integer or any real number. The default is TRUE. If you set integer offset? to FALSE, the group delay offset is any real number. Using an integer can slightly increase delay. order — order specifies the filter order. The value of order must be greater than zero. The default is 10. band specs — band specs specifies the group delay response you want for the filter, for one or more frequency bands. subband specs — specifies group delays in each sub-band. freq — freq specifies the frequency point in hertz. group delay — group delay specifies the group delay you want for the frequency point. weight — weight defines the relative significance of the group delay you want. Increasing the value of weight adds precision to group delay for the frequency point. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. filter out — filter out returns a new filter. mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — order specifies the filter order. The value of order must be greater than zero. The default is 10. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. offset — offset returns the group delay offset between the group delay of filter out and the group delay specified in band specs. The integer offset? input determines whether offset is an integer or a floating-point number. error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| subband specs — specifies group delays in each sub-band. freq — freq specifies the frequency point in hertz. group delay — group delay specifies the group delay you want for the frequency point. weight — weight defines the relative significance of the group delay you want. Increasing the value of weight adds precision to group delay for the frequency point. |
| freq — freq specifies the frequency point in hertz. group delay — group delay specifies the group delay you want for the frequency point. weight — weight defines the relative significance of the group delay you want. Increasing the value of weight adds precision to group delay for the frequency point. |
| mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — order specifies the filter order. The value of order must be greater than zero. The default is 10. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |

#### Examples

Refer to the Arbitrary Group Delay Filter Design VI in the labview\examples\Digital Filter Design\Floating-Point Filters\Conventional directory for an example of using the DFD Arbitrary Group Delay Design VI.

Parent topic:

Special Filter Design

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-bessel-design-vi.html language=enus -->
## TOPIC 00003: DFD Bessel Design VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-bessel-design-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-bessel-design-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Creates a digital Bessel infinite impulse response (IIR) filter. icon Inputs/Outputs cenum.png filter type filter type specifies the type of filter that this VI creates. ci32.png order order specifies the digital filter order. The value of order must be greater than zero. The default is 2. If you se

### DFD Bessel Design VI

Creates a digital Bessel infinite impulse response (IIR) filter.

[IMAGE alt='icon' src='dfd-bessel-design-vi.png']

#### Inputs/Outputs

| filter type — filter type specifies the type of filter that this VI creates. order — order specifies the digital filter order. The value of order must be greater than zero. The default is 2. If you set filter type to Lowpass or Highpass, the digital filter order you specify must equal the analog prototype filter order. If you set filter type to Bandstop or Bandpass, the digital filter order you specify must be an even number that equals two times the analog prototype filter order. Increasing the value can narrow the transition band. high cutoff freq [Hz] — high cutoff freq specifies the high cutoff frequency in hertz. The value must be greater than low cutoff freq. The default is 0.45. This VI uses this input for bandpass and bandstop filter designs only. The cutoff frequency specifies the passband, or the region of an approximately linear phase response. low cutoff freq [Hz] — low cutoff freq specifies the low cutoff frequency in hertz. The default is 0.12. The cutoff frequency specifies the passband, or the region of an approximately linear phase response. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. filter out — filter out returns a new filter. mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — order specifies the digital filter order. The value of order must be greater than zero. The default is 2. If you set filter type to Lowpass or Highpass, the digital filter order you specify must equal the analog prototype filter order. If you set filter type to Bandstop or Bandpass, the digital filter order you specify must be an even number that equals two times the analog prototype filter order. Increasing the value can narrow the transition band. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — order specifies the digital filter order. The value of order must be greater than zero. The default is 2. If you set filter type to Lowpass or Highpass, the digital filter order you specify must equal the analog prototype filter order. If you set filter type to Bandstop or Bandpass, the digital filter order you specify must be an even number that equals two times the analog prototype filter order. Increasing the value can narrow the transition band. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |

#### DFD Bessel Design Details

Bessel filters have maximally flat response in both the passband and the stopband. The phase response in the passband of Bessel filters, which typically is the region of interest, is nearly linear. However, Bessel filters require high-order filters to provide a good approximation of the ideal filter response.

To achieve linear phase response without a Bessel filter, use FIR filter designs or use the [DFD Group Delay Compensator](dfd-group-delay-compensator-vi.html) VI to compensate the IIR filter group delay.

#### Examples

Refer to the following VIs for examples of using the DFD Bessel Design VI:

- IIR Filter Design - DFD VI: labview\examples\Digital Filter Design\AALXMPL
- Bessel IIR Filter Design VI: labview\examples\Digital Filter Design\Floating-Point Filters\Conventional

Parent topic:

Advanced IIR Filter Design

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-butterworth-design-vi.html language=enus -->
## TOPIC 00004: DFD Butterworth Design VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-butterworth-design-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-butterworth-design-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Creates a digital Butterworth infinite impulse response (IIR) filter. You can use the DFD Butterworth Order Estimation VI to estimate order. icon Inputs/Outputs cenum.png filter type filter type specifies the type of filter that this VI creates. ci32.png order order specifies the digital filter orde

### DFD Butterworth Design VI

Creates a digital Butterworth infinite impulse response (IIR) filter.

You can use the [DFD Butterworth Order Estimation](dfd-butterworth-order-estimation-vi.html) VI to estimate **order**.

[IMAGE alt='icon' src='dfd-butterworth-design-vi.png']

#### Inputs/Outputs

| filter type — filter type specifies the type of filter that this VI creates. order — order specifies the digital filter order. The value of order must be greater than zero. The default is 2. If you set filter type to Lowpass or Highpass, the digital filter order you specify must equal the analog prototype filter order. If you set filter type to Bandstop or Bandpass, the digital filter order you specify must be an even number that equals two times the analog prototype filter order. Increasing the value can narrow the transition band. high cutoff freq [Hz] — high cutoff freq specifies the high cutoff frequency in hertz. The value must be greater than low cutoff freq. The default is 0.45. This VI uses this input only for bandpass and bandstop filter design. The cutoff frequency that you specify corresponds to the half-power frequency or the 3 dB frequency. low cutoff freq [Hz] — low cutoff freq specifies the low cutoff frequency in hertz. The default is 0.12. The cutoff frequency that you specify corresponds to the half-power frequency or the 3 dB frequency. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. filter out — filter out returns a new filter. mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — order specifies the digital filter order. The value of order must be greater than zero. The default is 2. If you set filter type to Lowpass or Highpass, the digital filter order you specify must equal the analog prototype filter order. If you set filter type to Bandstop or Bandpass, the digital filter order you specify must be an even number that equals two times the analog prototype filter order. Increasing the value can narrow the transition band. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — order specifies the digital filter order. The value of order must be greater than zero. The default is 2. If you set filter type to Lowpass or Highpass, the digital filter order you specify must equal the analog prototype filter order. If you set filter type to Bandstop or Bandpass, the digital filter order you specify must be an even number that equals two times the analog prototype filter order. Increasing the value can narrow the transition band. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |

#### DFD Butterworth Design Details

Butterworth filters have the following characteristics:

- Smooth frequency response at all frequencies and monotonically decreasing or increasing magnitude response in the transition band.
- Maximally flat at frequencies of zero and one-half the sampling frequency.

Butterworth filters do not always provide an acceptable accurate approximation of the ideal frequency response because the filter has slow roll off in the transition band. Use Chebyshev, Inverse Chebyshev, or Elliptic filters to achieve a sharper roll off.

#### Examples

Refer to the following VIs for examples of using the DFD Butterworth Design VI:

- Extract the Sine Wave - DFD VI: labview\examples\Digital Filter Design\AALXMPL
- IIR Filter Design - DFD VI: labview\examples\Digital Filter Design\AALXMPL

Parent topic:

Advanced IIR Filter Design

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-butterworth-order-estimation-vi.html language=enus -->
## TOPIC 00005: DFD Butterworth Order Estimation VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-butterworth-order-estimation-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-butterworth-order-estimation-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Estimates the Butterworth filter order. icon Inputs/Outputs cenum.png filter type filter type specifies the type of filter that this VI creates. cnclst.png freq specs [Hz] freq specs specifies the band edge frequencies of the filter in hertz. cdbl.png fpass 1 fpass 1 specifies the first passband edg

### DFD Butterworth Order Estimation VI

Estimates the Butterworth filter order.

[IMAGE alt='icon' src='dfd-butterworth-order-estimation-vi.png']

#### Inputs/Outputs

| filter type — filter type specifies the type of filter that this VI creates. freq specs [Hz] — freq specs specifies the band edge frequencies of the filter in hertz. fpass 1 — fpass 1 specifies the first passband edge frequency in hertz. fstop 1 — fstop 1 specifies the first stopband edge frequency in hertz. fpass 2 — fpass 2 specifies the second passband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. fstop 2 — fstop 2 specifies the second stopband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. ripple specs — ripple specs specifies the ripple level in the passband and stopband of the filter. passband — passband specifies the ripple level in the passband. The default is 0.1. stopband — stopband specifies the ripple level in the stopband. The default is 60. dB/linear? (T: dB) — dB/linear? specifies whether this VI applies a decibel scale or a linear scale to the ripple levels. If the value is TRUE, this VI applies a decibel scale to the ripple level. If the value is FALSE, this VI applies a linear scale to the ripple level. The default is TRUE. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. estimated order — estimated order returns the minimum order value that the filter requires to meet the specifications you set. high cutoff freq [Hz] — high cutoff freq returns the high cutoff frequency. The cutoff frequency corresponds to the half-power frequency or the 3 dB frequency. low cutoff freq [Hz] — low cutoff freq returns the low cutoff frequency. The cutoff frequency corresponds to the half-power frequency or the 3 dB frequency. error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| fpass 1 — fpass 1 specifies the first passband edge frequency in hertz. fstop 1 — fstop 1 specifies the first stopband edge frequency in hertz. fpass 2 — fpass 2 specifies the second passband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. fstop 2 — fstop 2 specifies the second stopband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. |
| passband — passband specifies the ripple level in the passband. The default is 0.1. stopband — stopband specifies the ripple level in the stopband. The default is 60. dB/linear? (T: dB) — dB/linear? specifies whether this VI applies a decibel scale or a linear scale to the ripple levels. If the value is TRUE, this VI applies a decibel scale to the ripple level. If the value is FALSE, this VI applies a linear scale to the ripple level. The default is TRUE. |

Parent topic:

Advanced IIR Filter Design

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-chebyshev-design-vi.html language=enus -->
## TOPIC 00006: DFD Chebyshev Design VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-chebyshev-design-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-chebyshev-design-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Creates a digital Chebyshev infinite impulse response (IIR) filter. You can use the DFD Chebyshev Order Estimation VI to estimate order. icon Inputs/Outputs cenum.png filter type filter type specifies the type of filter that this VI creates. ci32.png order order specifies the digital filter order. T

### DFD Chebyshev Design VI

Creates a digital Chebyshev infinite impulse response (IIR) filter.

You can use the [DFD Chebyshev Order Estimation](dfd-chebyshev-order-estimation-vi.html) VI to estimate **order**.

[IMAGE alt='icon' src='dfd-chebyshev-design-vi.png']

#### Inputs/Outputs

| filter type — filter type specifies the type of filter that this VI creates. order — order specifies the digital filter order. The value of order must be greater than zero. The default is 2. If you set filter type to Lowpass or Highpass, the digital filter order you specify must equal the analog prototype filter order. If you set filter type to Bandstop or Bandpass, the digital filter order you specify must be an even number that equals two times the analog prototype filter order. Increasing the value can narrow the transition band. high cutoff freq [Hz] — high cutoff freq specifies the high cutoff frequency in hertz. The value must be greater than low cutoff freq. The default is 0.45. This VI uses this input only for bandpass and bandstop filter design. The cutoff frequency that you specify corresponds to the edge frequency of the passband. low cutoff freq [Hz] — low cutoff freq specifies the low cutoff frequency in hertz. The default is 0.12. The cutoff frequency that you specify corresponds to the edge frequency of the passband. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. passband ripple [dB] — passband ripple specifies the ripple level in the passband in decibels. The value must be greater than zero. The default is 1. filter out — filter out returns a new filter. mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — order specifies the digital filter order. The value of order must be greater than zero. The default is 2. If you set filter type to Lowpass or Highpass, the digital filter order you specify must equal the analog prototype filter order. If you set filter type to Bandstop or Bandpass, the digital filter order you specify must be an even number that equals two times the analog prototype filter order. Increasing the value can narrow the transition band. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — order specifies the digital filter order. The value of order must be greater than zero. The default is 2. If you set filter type to Lowpass or Highpass, the digital filter order you specify must equal the analog prototype filter order. If you set filter type to Bandstop or Bandpass, the digital filter order you specify must be an even number that equals two times the analog prototype filter order. Increasing the value can narrow the transition band. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |

#### DFD Chebyshev Design Details

Chebyshev filters have the following characteristics:

- Minimum peak error in the passband.
- Equi-ripple magnitude response in the passband.
- Monotonically decreasing or increasing magnitude response in the transition band and the stopband.
- For filters of the same order, the roll off in the transition band is sharper than that of Butterworth filters.

#### Examples

Refer to the IIR Filter Design - DFD VI in the labview\examples\Digital Filter Design\AALXMPL directory for an example of using the DFD Chebyshev Design VI.

Parent topic:

Advanced IIR Filter Design

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-chebyshev-order-estimation-vi.html language=enus -->
## TOPIC 00007: DFD Chebyshev Order Estimation VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-chebyshev-order-estimation-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-chebyshev-order-estimation-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Estimates the Chebyshev I filter order. icon Inputs/Outputs cenum.png filter type filter type specifies the type of filter that this VI creates. cnclst.png freq specs [Hz] freq specs specifies the band edge frequencies of the filter in hertz. cdbl.png fpass 1 fpass 1 specifies the first passband edg

### DFD Chebyshev Order Estimation VI

Estimates the Chebyshev I filter order.

[IMAGE alt='icon' src='dfd-chebyshev-order-estimation-vi.png']

#### Inputs/Outputs

| filter type — filter type specifies the type of filter that this VI creates. freq specs [Hz] — freq specs specifies the band edge frequencies of the filter in hertz. fpass 1 — fpass 1 specifies the first passband edge frequency in hertz. fstop 1 — fstop 1 specifies the first stopband edge frequency in hertz. fpass 2 — fpass 2 specifies the second passband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. fstop 2 — fstop 2 specifies the second stopband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. ripple specs — ripple specs specifies the ripple level in the passband and stopband of the filter. passband — passband specifies the ripple level in the passband. The default is 0.1. stopband — stopband specifies the ripple level in the stopband. The default is 60. dB/linear? (T: dB) — dB/linear? specifies whether this VI applies a decibel scale or a linear scale to the ripple levels. If the value is TRUE, this VI applies a decibel scale to the ripple level. If the value is FALSE, this VI applies a linear scale to the ripple level. The default is TRUE. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. estimated order — estimated order returns the minimum order value that the filter requires to meet the specifications you set. high cutoff freq [Hz] — high cutoff freq returns the high cutoff frequency. The cutoff frequency corresponds to the edge frequency of the passband. low cutoff freq [Hz] — low cutoff freq returns the low cutoff frequency. The cutoff frequency corresponds to the edge frequency of the passband. error out — error out contains error information. This output provides standard error out functionality. passband ripple [dB] — passband ripple returns the ripple level in the passband in decibels. |
| --- |
| fpass 1 — fpass 1 specifies the first passband edge frequency in hertz. fstop 1 — fstop 1 specifies the first stopband edge frequency in hertz. fpass 2 — fpass 2 specifies the second passband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. fstop 2 — fstop 2 specifies the second stopband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. |
| passband — passband specifies the ripple level in the passband. The default is 0.1. stopband — stopband specifies the ripple level in the stopband. The default is 60. dB/linear? (T: dB) — dB/linear? specifies whether this VI applies a decibel scale or a linear scale to the ripple levels. If the value is TRUE, this VI applies a decibel scale to the ripple level. If the value is FALSE, this VI applies a linear scale to the ripple level. The default is TRUE. |

Parent topic:

Advanced IIR Filter Design

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-classical-design-vi.html language=enus -->
## TOPIC 00008: DFD Classical Design VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-classical-design-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-classical-design-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Creates a lowpass, highpass, bandpass, or bandstop filter. icon Inputs/Outputs cenum.png design method design method specifies the method that this VI uses to design the filter. cenum.png filter type filter type specifies the type of filter that this VI creates. cnclst.png freq specs [Hz] freq specs

### DFD Classical Design VI

Creates a lowpass, highpass, bandpass, or bandstop filter.

[IMAGE alt='icon' src='dfd-classical-design-vi.png']

#### Inputs/Outputs

| design method — design method specifies the method that this VI uses to design the filter. filter type — filter type specifies the type of filter that this VI creates. freq specs [Hz] — freq specs specifies the band edge frequencies of the filter in hertz. fpass 1 — fpass 1 specifies the first passband edge frequency in hertz. fstop 1 — fstop 1 specifies the first stopband edge frequency in hertz. fpass 2 — fpass 2 specifies the second passband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. fstop 2 — fstop 2 specifies the second stopband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. ripple specs — ripple specs specifies the ripple level in the passband and stopband of the filter. passband — passband specifies the ripple level in the passband. The default is 0.1. stopband — stopband specifies the ripple level in the stopband. The default is 60. dB/linear? (T: dB) — dB/linear? specifies whether this VI applies a decibel scale or a linear scale to the ripple levels. If the value is TRUE, this VI applies a decibel scale to the ripple level. If the value is FALSE, this VI applies a linear scale to the ripple level. The default is TRUE. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. filter out — filter out returns a new filter. mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — source describes the origin of the error or warning and is, in most cases, the name of the node that produced the error or warning. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — filter out returns a new filter. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. order — order returns the filter order. error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| fpass 1 — fpass 1 specifies the first passband edge frequency in hertz. fstop 1 — fstop 1 specifies the first stopband edge frequency in hertz. fpass 2 — fpass 2 specifies the second passband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. fstop 2 — fstop 2 specifies the second stopband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. |
| passband — passband specifies the ripple level in the passband. The default is 0.1. stopband — stopband specifies the ripple level in the stopband. The default is 60. dB/linear? (T: dB) — dB/linear? specifies whether this VI applies a decibel scale or a linear scale to the ripple levels. If the value is TRUE, this VI applies a decibel scale to the ripple level. If the value is FALSE, this VI applies a linear scale to the ripple level. The default is TRUE. |
| mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — source describes the origin of the error or warning and is, in most cases, the name of the node that produced the error or warning. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — filter out returns a new filter. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — source describes the origin of the error or warning and is, in most cases, the name of the node that produced the error or warning. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |

#### Examples

Refer to the Classical Filter Design VI in the labview\examples\Digital Filter Design\Floating-Point Filters\Conventional directory for an example of using the DFD Classical Design VI.

Parent topic:

Filter Design

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-dolph-chebyshev-design-vi.html language=enus -->
## TOPIC 00009: DFD Dolph-Chebyshev Design VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-dolph-chebyshev-design-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-dolph-chebyshev-design-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Creates a finite impulse response (FIR) filter by using the Dolph-Chebyshev Window method. You can use the DFD Dolph-Chebyshev Order Estimation VI to estimate order. icon Inputs/Outputs cenum.png filter type filter type specifies the type of filter that this VI creates. ci32.png order order specifie

### DFD Dolph-Chebyshev Design VI

Creates a finite impulse response (FIR) filter by using the Dolph-Chebyshev Window method.

You can use the [DFD Dolph-Chebyshev Order Estimation](dfd-dolph-chebyshev-order-estimation-vi.html) VI to estimate **order**.

[IMAGE alt='icon' src='dfd-dolph-chebyshev-design-vi.png']

#### Inputs/Outputs

| filter type — filter type specifies the type of filter that this VI creates. order — order specifies the filter order. The value of order must be greater than zero. The default is 31. order +1 equals the number of coefficients or filter taps. Increasing the value can narrow the transition band. high cutoff freq [Hz] — high cutoff freq specifies the high cutoff frequency in hertz. The value must be greater than low cutoff freq. The default is 0.45. This VI uses this input only for bandpass and bandstop filter design. The cutoff frequency is the frequency with one-half magnitude response. low cutoff freq [Hz] — low cutoff freq specifies the low cutoff frequency in hertz. The default is 0.12. The cutoff frequency is the frequency with one-half magnitude response. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. ripple ratio [dB] — ripple ratio specifies the side-lobe attenuation provided by the Dolph-Chebyshev Window function in decibels. The value must be greater than zero. The default is 40. Increasing the value of ripple ratio decreases the ripples in the passband and stopband. filter out — filter out returns a new filter. mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — source describes the origin of the error or warning and is, in most cases, the name of the node that produced the error or warning. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — order specifies the filter order. The value of order must be greater than zero. The default is 31. order +1 equals the number of coefficients or filter taps. Increasing the value can narrow the transition band. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| mSRate_Fs — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. mSRate_Version — filter out returns a new filter. mSRate_Comments — filter out returns a new filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a new filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a new filter. source — source describes the origin of the error or warning and is, in most cases, the name of the node that produced the error or warning. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — order specifies the filter order. The value of order must be greater than zero. The default is 31. order +1 equals the number of coefficients or filter taps. Increasing the value can narrow the transition band. mSRate_Scaling — filter out returns a new filter. mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — source describes the origin of the error or warning and is, in most cases, the name of the node that produced the error or warning. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — filter out returns a new filter. mSRate_ScaleType — filter out returns a new filter. |

#### DFD Dolph-Chebyshev Design Details

The Dolph-Chebyshev Window is a ripple-adjustable window, as described in the following equation:

[IMAGE alt='image' src='eq1_dolph_cheb.gif']

where r is the relative side-lobe attenuation of the Dolph-Chebyshev Window in decibels, and C<sub>M</sub>(x) is the M<sup>th</sup> order Chebyshev polynomial.

Parent topic:

Advanced FIR Filter Design

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-dolph-chebyshev-order-estimation-vi.html language=enus -->
## TOPIC 00010: DFD Dolph-Chebyshev Order Estimation VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-dolph-chebyshev-order-estimation-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-dolph-chebyshev-order-estimation-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Estimates the order of a finite impulse response (FIR) filter that you designed by using the Dolph-Chebyshev Window method. icon Inputs/Outputs cenum.png filter type filter type specifies the type of filter that this VI creates. cnclst.png freq specs [Hz] freq specs specifies the band edge frequenci

### DFD Dolph-Chebyshev Order Estimation VI

Estimates the order of a finite impulse response (FIR) filter that you designed by using the Dolph-Chebyshev Window method.

[IMAGE alt='icon' src='dfd-dolph-chebyshev-order-estimation-vi.png']

#### Inputs/Outputs

| filter type — filter type specifies the type of filter that this VI creates. freq specs [Hz] — freq specs specifies the band edge frequencies of the filter in hertz. fpass 1 — fpass 1 specifies the first passband edge frequency in hertz. fstop 1 — fstop 1 specifies the first stopband edge frequency in hertz. fpass 2 — fpass 2 specifies the second passband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. fstop 2 — fstop 2 specifies the second stopband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. ripple specs — ripple specs specifies the ripple level in the passband and stopband of the filter. passband — passband specifies the ripple level in the passband. The default is 0.1. stopband — stopband specifies the ripple level in the stopband. The default is 60. dB/linear? (T: dB) — dB/linear? specifies whether this VI applies a decibel scale or a linear scale to the ripple levels. If the value is TRUE, this VI applies a decibel scale to the ripple level. If the value is FALSE, this VI applies a linear scale to the ripple level. The default is TRUE. order option — order option specifies filter order requirements. The default is MinEven, which yields the minimum even order for the filter to meet the specifications you set. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. estimated order — estimated order returns the minimum order value that the filter requires to meet the specifications you set. high cutoff freq [Hz] — high cutoff freq returns the high cutoff frequency in hertz. The cutoff frequency is the frequency with one-half magnitude response. low cutoff freq [Hz] — low cutoff freq returns the low cutoff frequency in hertz. The cutoff frequency is the frequency with one-half magnitude response. error out — error out contains error information. This output provides standard error out functionality. ripple ratio [dB] — ripple ratio returns the side-lobe attenuation provided by the Dolph-Chebyshev Window in decibels. |
| --- |
| fpass 1 — fpass 1 specifies the first passband edge frequency in hertz. fstop 1 — fstop 1 specifies the first stopband edge frequency in hertz. fpass 2 — fpass 2 specifies the second passband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. fstop 2 — fstop 2 specifies the second stopband edge frequency in hertz. This VI ignores this input for lowpass and highpass filters. |
| passband — passband specifies the ripple level in the passband. The default is 0.1. stopband — stopband specifies the ripple level in the stopband. The default is 60. dB/linear? (T: dB) — dB/linear? specifies whether this VI applies a decibel scale or a linear scale to the ripple levels. If the value is TRUE, this VI applies a decibel scale to the ripple level. If the value is FALSE, this VI applies a linear scale to the ripple level. The default is TRUE. |

Parent topic:

Advanced FIR Filter Design

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-iir-notch-peak-design-by-bw-vi.html language=enus -->
## TOPIC 00011: DFD IIR Notch Peak Design by BW VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-iir-notch-peak-design-by-bw-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-iir-notch-peak-design-by-bw-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Designs a notch or peak filter in which the notch or peak is located at the center frequency. You must manually select the polymorphic instance you want to use. Use the pull-down menu to select an instance of this VI. icon Inputs/Outputs cenum.png filter type filter type specifies the type of filter

### DFD IIR Notch Peak Design by BW VI

Designs a notch or peak filter in which the notch or peak is located at the center frequency. You must manually select the polymorphic instance you want to use.

Use the pull-down menu to select an instance of this VI.

[IMAGE alt='icon' src='dfd-iir-notch-peak-design-by-bw-vi.png']

#### Inputs/Outputs

| filter type — filter type specifies the type of filter that this VI creates. f0 [Hz] — f0 specifies the center frequency of the notch or peak in hertz. The value of f0 must be greater than or equal to 0 but less than half the value of fs. The default is 0.10. Df [Hz] — Df defines the full bandwidth at the level of -Ab in hertz. The default is 0.02. Ab [dB] — Ab specifies the attenuation, in decibels, that corresponds to the bandwidth. The default is 3.0103, which corresponds to 3 dB bandwidth, the commonly used bandwidth of a filter. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. filter out — filter out returns a new filter. mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |

#### Examples

Refer to the following VIs for examples of using the DFD IIR Notch Peak Design VI:

- IIR Notch Peak Filter Design VI: labview\examples\Digital Filter Design\Floating-Point Filters\Conventional
- Noise Cancellation for ECG Signal by Notch Filter VI: labview\examples\Digital Filter Design\Floating-Point Filters\Conventional

Parent topic:

DFD IIR Notch Peak Design VI

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/design-llb/dfd-iir-notch-peak-design-by-q-vi.html language=enus -->
## TOPIC 00012: DFD IIR Notch Peak Design by Q VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/design-llb/dfd-iir-notch-peak-design-by-q-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/design-llb/dfd-iir-notch-peak-design-by-q-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Designs a notch or peak filter in which the notch or peak is located at the center frequency. You must manually select the polymorphic instance you want to use. Use the pull-down menu to select an instance of this VI. icon Inputs/Outputs cenum.png filter type filter type specifies the type of filter

### DFD IIR Notch Peak Design by Q VI

Designs a notch or peak filter in which the notch or peak is located at the center frequency. You must manually select the polymorphic instance you want to use.

Use the pull-down menu to select an instance of this VI.

[IMAGE alt='icon' src='dfd-iir-notch-peak-design-by-q-vi.png']

#### Inputs/Outputs

| filter type — filter type specifies the type of filter that this VI creates. f0 [Hz] — f0 specifies the center frequency of the notch or peak in hertz. The value of f0 must be greater than 0 but less than half the value of fs. The default is 0.10. Q factor — Q factor specifies the Q factor of the filter, which equals the quotient of center frequency and corresponding bandwidth. The default is 20. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. fs [Hz] — fs specifies the sampling frequency in hertz. The value must be greater than zero. The default is 1, which is the normalized sampling frequency. filter out — filter out returns a new filter. mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |

#### Examples

Refer to the following VIs for examples of using the DFD IIR Notch Peak Design VI:

- IIR Notch Peak Filter Design VI: labview\examples\Digital Filter Design\Floating-Point Filters\Conventional
- Noise Cancellation for ECG Signal by Notch Filter VI: labview\examples\Digital Filter Design\Floating-Point Filters\Conventional

Parent topic:

DFD IIR Notch Peak Design VI

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-get-quantizer-one-vi.html language=enus -->
## TOPIC 00013: DFD FXP Get Quantizer (One) VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-get-quantizer-one-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-get-quantizer-one-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Retrieves quantizer settings from a fixed-point filter. You must manually select the polymorphic instance you want to use. Use the pull-down menu to select an instance of this VI. icon Inputs/Outputs ccclst.png filter in filter in specifies the input filter. cdbl.png mSRate_Fs cstr.png mSRate_Versio

### DFD FXP Get Quantizer (One) VI

Retrieves quantizer settings from a fixed-point filter. You must manually select the polymorphic instance you want to use.

Use the pull-down menu to select an instance of this VI.

[IMAGE alt='icon' src='dfd-fxp-get-quantizer-one-vi.png']

#### Inputs/Outputs

| filter in — filter in specifies the input filter. mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — quantizer source — quantizer source specifies the source for the quantizer. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. filter out — filter out returns the filter in unchanged. mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — quantizer — quantizer returns the settings of one quantizer. source — source contains the quantizer source. wl — wl returns the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl returns the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode returns the operation mode for overflow and underflow in the quantizer. rounding mode — rounding mode returns the mode for rounding numbers in the quantizer. signed? — signed? is TRUE if the fixed-point number is a signed number. signed? is FALSE if the fixed-point number is an unsigned number. error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| source — source contains the quantizer source. wl — wl returns the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl returns the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode returns the operation mode for overflow and underflow in the quantizer. rounding mode — rounding mode returns the mode for rounding numbers in the quantizer. signed? — signed? is TRUE if the fixed-point number is a signed number. signed? is FALSE if the fixed-point number is an unsigned number. |

#### Examples

Refer to the Easy Fixed-Point Filter Modeling and Simulation VI in the labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate directory for an example of using the DFD FXP Get Quantizer VI.

Parent topic:

DFD FXP Get Quantizer VI

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-get-quantizer-vi.html language=enus -->
## TOPIC 00014: DFD FXP Get Quantizer VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-get-quantizer-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-get-quantizer-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Retrieves quantizer settings from a fixed-point filter. You must manually select the polymorphic instance you want to use. Use the pull-down menu to select an instance of this VI. icon Examples Refer to the Easy Fixed-Point Filter Modeling and Simulation VI in the labview\examples\Digital Filter Des

### DFD FXP Get Quantizer VI

Retrieves quantizer settings from a fixed-point filter. You must manually select the polymorphic instance you want to use.

Use the pull-down menu to select an instance of this VI.

[IMAGE alt='icon' src='dfd-fxp-get-quantizer-vi.png']

#### Examples

Refer to the Easy Fixed-Point Filter Modeling and Simulation VI in the labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate directory for an example of using the DFD FXP Get Quantizer VI.

- [DFD FXP Get Quantizer (All) VI](../../../../vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-get-quantizer-all-vi.html) Retrieves quantizer settings from a fixed-point filter. You must manually select the polymorphic instance you want to use.
- [DFD FXP Get Quantizer (One) VI](../../../../vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-get-quantizer-one-vi.html) Retrieves quantizer settings from a fixed-point filter. You must manually select the polymorphic instance you want to use.
- [DFD FXP Get Quantizer (Group) VI](../../../../vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-get-quantizer-group-vi.html) Retrieves quantizer settings from a fixed-point filter. You must manually select the polymorphic instance you want to use.

Parent topic:

Fixed-Point Tools

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-modeling-vi.html language=enus -->
## TOPIC 00015: DFD FXP Modeling VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-modeling-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-modeling-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Creates a fixed-point filter model according to the settings of the input word length, input integer word length, and output word length. To avoid errors in generating code from a fixed-point filter, ensure that you configure the quantizers according to the guidelines in the Details section of the D

### DFD FXP Modeling VI

Creates a fixed-point filter model according to the settings of the input word length, input integer word length, and output word length.

Note

Details

[IMAGE alt='image' src='DFD_FXP_Modeling.gif']

[IMAGE alt='icon' src='dfd-fxp-modeling-vi.png']

#### Inputs/Outputs

| output integer word length — output integer word length specifies the integer word length that this VI uses to represent the output signal. output option — output option specifies the method to decide the output integer word length. integer word length — integer word length specifies the integer word length, in number of bits, that this VI uses to represent the output signal. The default is 0. This input is only valid when the output option is User Defined. input integer word length — input integer word length specifies the integer word length, in number of bits, that this VI uses to represent the input signal. The default is 1. filter in — filter in specifies the input filter. mSRate_Fs — filter in specifies the input filter. mSRate_Version — filter in specifies the input filter. mSRate_Comments — filter in specifies the input filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter in specifies the input filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter in specifies the input filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — filter in specifies the input filter. mSRate_Scaling — filter in specifies the input filter. mSRate_ScaleByPowerOf2? — filter in specifies the input filter. mSRate_ScaleType — filter in specifies the input filter. input word length — input word length specifies the word length, in number of bits, that this VI uses to represent the input signal. The valid range is [1, 32]. The default is 16. output word length — output word length specifies the word length, in number of bits, that this VI uses to represent the output signal. The valid range is [1, 32]. The default is 16. error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. output rounding mode — output rounding mode specifies the rounding mode this VI uses in the output quantizer. filter out — filter out returns a fixed-point filter. mSRate_Fs — filter out returns a fixed-point filter. mSRate_Version — filter out returns a fixed-point filter. mSRate_Comments — filter out returns a fixed-point filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a fixed-point filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a fixed-point filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — filter out returns a fixed-point filter. mSRate_Scaling — filter out returns a fixed-point filter. mSRate_ScaleByPowerOf2? — filter out returns a fixed-point filter. mSRate_ScaleType — filter out returns a fixed-point filter. error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| output option — output option specifies the method to decide the output integer word length. integer word length — integer word length specifies the integer word length, in number of bits, that this VI uses to represent the output signal. The default is 0. This input is only valid when the output option is User Defined. |
| mSRate_Fs — filter in specifies the input filter. mSRate_Version — filter in specifies the input filter. mSRate_Comments — filter in specifies the input filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter in specifies the input filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter in specifies the input filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — filter in specifies the input filter. mSRate_Scaling — filter in specifies the input filter. mSRate_ScaleByPowerOf2? — filter in specifies the input filter. mSRate_ScaleType — filter in specifies the input filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — filter in specifies the input filter. mSRate_ScaleType — filter in specifies the input filter. |
| mSRate_Fs — filter out returns a fixed-point filter. mSRate_Version — filter out returns a fixed-point filter. mSRate_Comments — filter out returns a fixed-point filter. mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — filter out returns a fixed-point filter. mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — filter out returns a fixed-point filter. source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — filter out returns a fixed-point filter. mSRate_Scaling — filter out returns a fixed-point filter. mSRate_ScaleByPowerOf2? — filter out returns a fixed-point filter. mSRate_ScaleType — filter out returns a fixed-point filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — filter out returns a fixed-point filter. mSRate_ScaleType — filter out returns a fixed-point filter. |

Parent topic:

Fixed-Point Tools

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-advanced-vi.html language=enus -->
## TOPIC 00016: DFD FXP Quantize Coef (Advanced) VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-advanced-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-advanced-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Quantizes the coefficients of a floating-point filter and generates a fixed-point filter. You must manually select the polymorphic instance you want to use. Use the DFD FXP Coef Report VI to generate a report on the effect that quantization has on the filter. Use the pull-down menu to select an inst

### DFD FXP Quantize Coef (Advanced) VI

Quantizes the coefficients of a floating-point filter and generates a fixed-point filter. You must manually select the polymorphic instance you want to use.

Use the [DFD FXP Coef Report](dfd-fxp-coef-report-vi.html) VI to generate a report on the effect that quantization has on the filter.

Use the pull-down menu to select an instance of this VI.

[IMAGE alt='icon' src='dfd-fxp-quantize-coef-advanced-vi.png']

#### Inputs/Outputs

| coefficients b/v quantizer — coefficients b/v quantizer specifies the settings for the coefficients b/v quantizer. source — source specifies the origin of the error or warning and is, in most cases, the name of the node that produced the error or warning. The default is an empty string. wl — wl specifies the word length, in number of bits, that the quantizer uses to represent a fixed-point number. The valid range is [1, 32]. If you specify an invalid value, this VI uses the same quantizer settings as you specified in the Coefficients b/v quantizer. The default is 16. iwl — iwl specifies the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. The default is 1. overflow mode — overflow mode specifies how this VI handles overflows and underflows in the quantizer. rounding mode — rounding mode specifies the rounding mode this VI uses in the quantizer. signed? — signed? specifies if the fixed-point number is a signed number. This VI supports signed numbers only. If you remove the checkmark from the signed? checkbox, the result you obtain might not be correct. coefficients a/k quantizer — coefficients a/k quantizer specifies the settings for the coefficients a/k quantizer. source — source specifies the quantizer source. wl — wl specifies the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl specifies the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. filter in — filter in specifies the input floating-point filter. mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. gain settings — gain settings specifies the settings for the filter gain. gain processing — gain processing specifies whether you want to process the filter gain on a host computer or a fixed-point target, such as an NI Reconfigurable I/O (RIO) target. If you want to generate C code from the resulting fixed-point filter, you must set gain processing to On Target. gain word length — gain word length specifies the word length, in number of bits, that this VI uses to represent the filter gain if you set gain processing to On Target. The valid range is [1, 32]. The default is 16. filter out — filter out returns a fixed-point filter. mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| source — source specifies the origin of the error or warning and is, in most cases, the name of the node that produced the error or warning. The default is an empty string. wl — wl specifies the word length, in number of bits, that the quantizer uses to represent a fixed-point number. The valid range is [1, 32]. If you specify an invalid value, this VI uses the same quantizer settings as you specified in the Coefficients b/v quantizer. The default is 16. iwl — iwl specifies the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. The default is 1. overflow mode — overflow mode specifies how this VI handles overflows and underflows in the quantizer. rounding mode — rounding mode specifies the rounding mode this VI uses in the quantizer. signed? — signed? specifies if the fixed-point number is a signed number. This VI supports signed numbers only. If you remove the checkmark from the signed? checkbox, the result you obtain might not be correct. |
| source — source specifies the quantizer source. wl — wl specifies the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl specifies the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| gain processing — gain processing specifies whether you want to process the filter gain on a host computer or a fixed-point target, such as an NI Reconfigurable I/O (RIO) target. If you want to generate C code from the resulting fixed-point filter, you must set gain processing to On Target. gain word length — gain word length specifies the word length, in number of bits, that this VI uses to represent the filter gain if you set gain processing to On Target. The valid range is [1, 32]. The default is 16. |
| mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |

#### DFD FXP Quantize Coef Details

You can represent an integer, or binary number, with a length **wl** as b<sub>1</sub>b<sub>2</sub>b<sub>3</sub>…b<sub>wl</sub> where b<sub>i</sub> is a binary digit. Both **wl** and **iwl** determine the position of the binary point and the range the binary number can represent.

For a signed fixed-point number in two's complement, the value of the fixed-point number is defined by the following equation:

[IMAGE alt='image' src='eq2_fxp_int.gif']

The highest representable number is 2<sup>iwl</sup>(2<sup>-1</sup>-2<sup>-wl</sup>) and the lowest representable number is -2<sup>iwl-1</sup>.

For an unsigned fixed-point number, the value of the fixed-point number is defined by the following equation:

[IMAGE alt='image' src='eq5_fxp_int.gif']

For unsigned fixed-point numbers, the highest representable number is 2<sup>iwl</sup>(1-2<sup>-wl</sup>) and the lowest representable number is 0.

The **rounding mode** input controls the operation of quantizing to **wl** bits. If you set **rounding mode** to Nearest, the quantizer rounds the result to the closest representable number. If the two closest representable numbers are equidistant, the quantizer rounds the result to the closest representable number with a least significant bit of 0. If you set **rounding mode** to Truncation, the quantizer rounds to the closest representable number lower than the original value.

**overflow mode** determines the quantized value when an overflow or underflow occurs. If you set **overflow mode** to Saturation, the quantizer converts the specified value to the highest representable number for overflow or to the lowest representable number for underflow. If you set **overflow mode** to Wrap, the quantizer wraps around the specified value from the highest representable number to the lowest representable number for overflow or from the lowest representable number to the highest representable number for underflow. The size of the error does not increase as abruptly with **Saturation** as the size does with **Wrap** when overflow or underflow occurs.

#### Examples

Refer to the following VIs for examples of using the DFD FXP Quantize Coef VI:

- How to Build Coefficients Quantizer VI: labview\examples\Digital Filter Design\Getting Started\Apply Filters
- Analyze Coefficients-Quantized Filter VI: labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate
- Easy Fixed-Point Filter Modeling and Simulation VI: labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate
- Structure Selection and Quantization VI: labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate

Parent topic:

DFD FXP Quantize Coef VI

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-easy-vi.html language=enus -->
## TOPIC 00017: DFD FXP Quantize Coef (Easy) VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-easy-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-easy-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Quantizes the coefficients of a floating-point filter and generates a fixed-point filter. You must manually select the polymorphic instance you want to use. Use the DFD FXP Coef Report VI to generate a report on the effect that quantization has on the filter. Use the pull-down menu to select an inst

### DFD FXP Quantize Coef (Easy) VI

Quantizes the coefficients of a floating-point filter and generates a fixed-point filter. You must manually select the polymorphic instance you want to use.

Use the [DFD FXP Coef Report](dfd-fxp-coef-report-vi.html) VI to generate a report on the effect that quantization has on the filter.

Use the pull-down menu to select an instance of this VI.

[IMAGE alt='icon' src='dfd-fxp-quantize-coef-easy-vi.png']

#### Inputs/Outputs

| coefficients b/v word length — coefficients b/v word length specifies the word length, in number of bits, that the quantizer uses to represent coefficients b/v. coefficients a/k word length — coefficients a/k word length specifies the word length, in number of bits, that the quantizer uses to represent coefficients a/k. filter in — filter in specifies the input floating-point filter. mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — error in (no error) — error in describes error conditions that occur before this node runs. This input provides standard error in functionality. gain settings — gain settings specifies the settings for the filter gain. gain processing — gain processing specifies whether you want to process the filter gain on a host computer or a fixed-point target, such as an NI Reconfigurable I/O (RIO) target. If you want to generate C code from the resulting fixed-point filter, you must set gain processing to On Target. gain word length — gain word length specifies the word length, in number of bits, that this VI uses to represent the filter gain if you set gain processing to On Target. The valid range is [1, 32]. The default is 16. filter out — filter out returns a fixed-point filter. mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — error out — error out contains error information. This output provides standard error out functionality. |
| --- |
| mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| gain processing — gain processing specifies whether you want to process the filter gain on a host computer or a fixed-point target, such as an NI Reconfigurable I/O (RIO) target. If you want to generate C code from the resulting fixed-point filter, you must set gain processing to On Target. gain word length — gain word length specifies the word length, in number of bits, that this VI uses to represent the filter gain if you set gain processing to On Target. The valid range is [1, 32]. The default is 16. |
| mSRate_Fs — mSRate_Version — mSRate_Comments — mSRate_OrgCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_CurCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_Quantized? — mSRate_QCoef — specifies a filter. type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. mSRate_QSettings — source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. mSRate_Order — mSRate_Scaling — mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| type — specifies structure of the filter. gain — specifies the gain of the filter. coef a (k) — specifies the forward coefficients of the filter. coef b (v) — specifies the reverse coefficients of the filter. |
| source — specifies the quantizer source. It has the following options: 0 - Coefficient 1 - Input 2 - Output 3 - Multiplicand 4 - Product 5 - Sum 6 - Delay The default is Coeffcient. wl — wl contains the word length, in number of bits, that the quantizer uses to represent a fixed-point number. iwl — iwl contains the integer word length, in number of bits, within wl that the quantizer uses to represent the integer part of a fixed-point number. overflow mode — overflow mode contains the setting that this VI uses to handle overflows and underflows in the quantizer. rounding mode — rounding mode contains the rounding mode this VI uses in the quantizer. signed? — signed? indicates if the fixed-point number is a signed number. |
| mSRate_ScaleByPowerOf2? — mSRate_ScaleType — |

#### DFD FXP Quantize Coef Details

You can represent an integer, or binary number, with a length **wl** as b<sub>1</sub>b<sub>2</sub>b<sub>3</sub>…b<sub>wl</sub> where b<sub>i</sub> is a binary digit. Both **wl** and **iwl** determine the position of the binary point and the range the binary number can represent.

For a signed fixed-point number in two's complement, the value of the fixed-point number is defined by the following equation:

[IMAGE alt='image' src='eq2_fxp_int.gif']

The highest representable number is 2<sup>iwl</sup>(2<sup>-1</sup>-2<sup>-wl</sup>) and the lowest representable number is -2<sup>iwl-1</sup>.

For an unsigned fixed-point number, the value of the fixed-point number is defined by the following equation:

[IMAGE alt='image' src='eq5_fxp_int.gif']

For unsigned fixed-point numbers, the highest representable number is 2<sup>iwl</sup>(1-2<sup>-wl</sup>) and the lowest representable number is 0.

The **rounding mode** input controls the operation of quantizing to **wl** bits. If you set **rounding mode** to Nearest, the quantizer rounds the result to the closest representable number. If the two closest representable numbers are equidistant, the quantizer rounds the result to the closest representable number with a least significant bit of 0. If you set **rounding mode** to Truncation, the quantizer rounds to the closest representable number lower than the original value.

**overflow mode** determines the quantized value when an overflow or underflow occurs. If you set **overflow mode** to Saturation, the quantizer converts the specified value to the highest representable number for overflow or to the lowest representable number for underflow. If you set **overflow mode** to Wrap, the quantizer wraps around the specified value from the highest representable number to the lowest representable number for overflow or from the lowest representable number to the highest representable number for underflow. The size of the error does not increase as abruptly with **Saturation** as the size does with **Wrap** when overflow or underflow occurs.

#### Examples

Refer to the following VIs for examples of using the DFD FXP Quantize Coef VI:

- How to Build Coefficients Quantizer VI: labview\examples\Digital Filter Design\Getting Started\Apply Filters
- Analyze Coefficients-Quantized Filter VI: labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate
- Easy Fixed-Point Filter Modeling and Simulation VI: labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate
- Structure Selection and Quantization VI: labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate

Parent topic:

DFD FXP Quantize Coef VI

<!--NI_TOPIC bundle=lvdfdt-api-ref path=vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-vi.html language=enus -->
## TOPIC 00018: DFD FXP Quantize Coef VI

- bundle_id: `lvdfdt-api-ref`
- source_path: `vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-vi.html`
- source_url: https://docs-be.ni.com/bundle/lvdfdt-api-ref/raw/resource/enus/vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-vi.html
- document_id: `lvdfdt-api-ref`
- page_type: `leaf`
- content_type: `reference`
- source_description: Quantizes the coefficients of a floating-point filter and generates a fixed-point filter. You must manually select the polymorphic instance you want to use. Use the DFD FXP Coef Report VI to generate a report on the effect that quantization has on the filter. Use the pull-down menu to select an inst

### DFD FXP Quantize Coef VI

Quantizes the coefficients of a floating-point filter and generates a fixed-point filter. You must manually select the polymorphic instance you want to use.

Use the [DFD FXP Coef Report](dfd-fxp-coef-report-vi.html) VI to generate a report on the effect that quantization has on the filter.

Use the pull-down menu to select an instance of this VI.

[IMAGE alt='icon' src='dfd-fxp-quantize-coef-vi.png']

#### DFD FXP Quantize Coef Details

You can represent an integer, or binary number, with a length **wl** as b<sub>1</sub>b<sub>2</sub>b<sub>3</sub>…b<sub>wl</sub> where b<sub>i</sub> is a binary digit. Both **wl** and **iwl** determine the position of the binary point and the range the binary number can represent.

For a signed fixed-point number in two's complement, the value of the fixed-point number is defined by the following equation:

[IMAGE alt='image' src='eq2_fxp_int.gif']

The highest representable number is 2<sup>iwl</sup>(2<sup>-1</sup>-2<sup>-wl</sup>) and the lowest representable number is -2<sup>iwl-1</sup>.

For an unsigned fixed-point number, the value of the fixed-point number is defined by the following equation:

[IMAGE alt='image' src='eq5_fxp_int.gif']

For unsigned fixed-point numbers, the highest representable number is 2<sup>iwl</sup>(1-2<sup>-wl</sup>) and the lowest representable number is 0.

The **rounding mode** input controls the operation of quantizing to **wl** bits. If you set **rounding mode** to Nearest, the quantizer rounds the result to the closest representable number. If the two closest representable numbers are equidistant, the quantizer rounds the result to the closest representable number with a least significant bit of 0. If you set **rounding mode** to Truncation, the quantizer rounds to the closest representable number lower than the original value.

**overflow mode** determines the quantized value when an overflow or underflow occurs. If you set **overflow mode** to Saturation, the quantizer converts the specified value to the highest representable number for overflow or to the lowest representable number for underflow. If you set **overflow mode** to Wrap, the quantizer wraps around the specified value from the highest representable number to the lowest representable number for overflow or from the lowest representable number to the highest representable number for underflow. The size of the error does not increase as abruptly with **Saturation** as the size does with **Wrap** when overflow or underflow occurs.

#### Examples

Refer to the following VIs for examples of using the DFD FXP Quantize Coef VI:

- How to Build Coefficients Quantizer VI: labview\examples\Digital Filter Design\Getting Started\Apply Filters
- Analyze Coefficients-Quantized Filter VI: labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate
- Easy Fixed-Point Filter Modeling and Simulation VI: labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate
- Structure Selection and Quantization VI: labview\examples\Digital Filter Design\Fixed-Point Filters\Single-Rate

- [DFD FXP Quantize Coef (Easy) VI](../../../../vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-easy-vi.html) Quantizes the coefficients of a floating-point filter and generates a fixed-point filter. You must manually select the polymorphic instance you want to use.
- [DFD FXP Quantize Coef (Advanced) VI](../../../../vi-lib/addons/digital-filter-design/fxpmodel-llb/dfd-fxp-quantize-coef-advanced-vi.html) Quantizes the coefficients of a floating-point filter and generates a fixed-point filter. You must manually select the polymorphic instance you want to use.

Parent topic:

Fixed-Point Tools
